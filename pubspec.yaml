name: wallet_exp
description: A TBD test wallet
publish_to: "none" # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.19.6 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  auto_route: ^7.4.0
  flutter_hooks: ^0.18.6
  flutter_inappwebview: ^5.7.2+3
  hooks_riverpod: ^2.3.6
  path_provider: ^2.0.15
  uni_links: ^0.5.1
  url_launcher: ^6.1.11
  webview_flutter: ^4.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  auto_route_generator: ^7.1.1
  build_runner: ^2.4.5
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/index.html
    - assets/scripts.js
    - assets/web5.js
    - assets/images/
